<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Customize Offers section -->
    <!-- Pressed navigation buttons -->
    <color name="wk_navbar_pressed">@color/wk_light_pressed_layer</color>
    <!-- Action buttons color -->
    <color name="wk_action_active">@color/wk_primary_pressed</color>
    <!-- ActionBar Logo -->
    <drawable name="wk_actionbar_logo">@drawable/wk_ic_btn_leisure</drawable>
    <!-- No titles -->
    <string name="wk_activity_offers">@null</string>

    <!-- Tag buttons -->
    <color name="wk_tag_active_bg">@color/wk_primary_pressed</color>
    <color name="wk_tag_pressed_bg">@color/wk_primary</color>
    <color name="wk_tag_active_border">@color/wk_primary</color>
    <color name="wk_tag_pressed_border">@color/wk_primary</color>

</resources>